import 'package:flutter/material.dart';

class FindTherapists extends StatelessWidget {
  const FindTherapists({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Therapists Screen"),
    );
  }
}
